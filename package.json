{
  "scripts": {
    "gen": "bun ergogen && bun points",
    "ergogen": "ergogen src/ --debug --output dist/",
    "points": "bun run ./scripts/openscad_points.ts",
    "fmt": "prettier --write ."
  },
  "dependencies": {
    "ergogen": "^4.1.0",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "prettier": "^3.7.4"
  }
}
