{
  "scripts": {
    "gen": "bun ergogen && bun pcb && bun points",
    "ergogen": "ergogen src/ --debug --output dist/",
    "pcb": "kicad-cli pcb export svg dist/pcbs/tsuki.kicad_pcb --layers 'F.Adhesive' --exclude-drawing-sheet --output dist/pcbs/cutouts.svg",
    "points": "bun run ./scripts/openscad_points.ts",
    "fmt": "prettier --write ."
  },
  "dependencies": {
    "ergogen": "^4.1.0",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "prettier": "^3.7.4"
  }
}
